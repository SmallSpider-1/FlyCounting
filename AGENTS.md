# 项目长期背景读取规则

> 本文件是本项目对所有 AI 编码助手的唯一规则来源。根目录 `CLAUDE.md` 只是指向本文件的入口，不重复规则内容；修改规则只改本文件。

处理本项目中与以下内容相关的任何任务前：

- 学位论文及两篇小论文；
- 实蝇目标检测；
- 多目标跟踪；
- 真实诱捕长视频分类计数；
- 数据集与实验设计；
- 树莓派部署和性能优化；

必须先完整阅读以下两份**必读文档**：

1. `research_docs/研究背景与论文主线.md` —— 长期研究主线和背景；
2. `research_docs/当前进展与下一步.md` —— 当前状态、待决策项和下一步（下称"状态文档"）。

以下两份为**按需文档**，不要在每个会话中通读，只在需要追溯具体历史时查阅：

- `research_docs/变更日志.md` —— 历史决策与依据，只追加；
- `research_docs/数据集沿革归档.md` —— C/R1/`dataset_det_2` 的清理审计过程，其中的中间统计多已被取代，引用前务必核对日期。

## 执行要求

- 将两份必读文档作为本项目长期背景和当前状态的来源；按需文档只在被明确需要时读取。
- 不要要求用户重复文档中已经记录的研究背景。
- 除非用户主动询问，否则无需在每个新会话中复述全部背景，读取后直接处理当前任务。
- 文档之间冲突时的优先级：状态文档 > `研究背景与论文主线.md` > 按需文档；同一文档内以日期较新的记录为准。发现按需文档与状态文档冲突时，以状态文档为准，并在变更日志中说明。
- 如果文档与用户当前消息冲突，以用户当前消息为准。
- 如果发现关键背景缺失或无法判断的矛盾，应先明确说明，再继续处理任务。
- 不得将文档中的“计划”、“待验证”、“候选方案”误写成已完成工作或已证实结论。
- 完成会改变研究方案、实验结论、数据定义、论文主线或下一步优先级的工作后，应同步更新对应背景文档。
- 更新状态文档 `research_docs/当前进展与下一步.md` 时遵守以下写法约束：
  - 该文档只写"现在是什么样"和"下一步做什么"，**正文（第 1 节到第 7 节，即第 8 节标题之前）必须保持在 120 行以内**；新增内容要靠替换和删除已过期内容腾出位置，不允许只增不减。
  - 改完必须用下面这条命令自检，输出的行号必须小于 122（第 8 节标题行号 = 正文行数 + 1）：

    ```bash
    grep -n "^## 8" research_docs/当前进展与下一步.md
    ```

    若超限，先把过期内容搬进变更日志或合并压缩，不要以"内容重要"为理由突破上限。
  - 历史过程、已被取代的数字、当日决策记录一律追加到 `research_docs/变更日志.md`，不留在状态文档里。
  - 发现旧记录与实际不符时直接改正，不得同时保留两种互相矛盾的说法；改正原因写入变更日志。
  - 引用目录、脚本或配置路径前先用 `ls`/`grep` 确认其存在，不要沿用已删除的路径；引用具体数字前先核对数据源，不要照抄旧文档。
- 追加 `research_docs/变更日志.md` 条目时遵守以下格式：
  - 按日期倒序，最新日期在最前；同一天的内容合并在同一个 `## YYYY-MM-DD` 标题下，需要时用 `### 小标题` 分组。
  - 每条是一个 `-` 列表项，写清"做了什么 + 依据/验证结果"；只写已经发生并核实过的事，不写计划。
  - 已失效的历史条目不删除，在其下补一条 `**本条已失效（YYYY-MM-DD）**：...` 说明，保留原始记录以便追溯。
- Codex 本地记忆只能作为辅助信息，不得替代本文件及上述两份研究文档。
