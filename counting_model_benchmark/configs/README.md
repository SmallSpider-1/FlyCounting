# Counting configurations

`baseline_circle_v1.json` is the current runnable starting point. It fixes the
single-circle, bounding-box-center, signed crossing rule. It is not a frozen
paper baseline and does not claim an accuracy improvement.

Create a new, separately named JSON file for each future ablation. Never edit a
configuration after results have been reported; copy it to a new version instead.
