# 数据集沿革归档

> 文档性质：过程记录归档。新会话**不需要**阅读本文档。
> 当前实际使用的检测数据只有 `datasets/fruitfly_detection_v1/`，其权威定义、统计和指纹见 `当前进展与下一步.md` 第 2 节，不以本文档为准。
> 本文档保留 C/R1/`dataset_det_2` 的清理审计过程与已删除的边缘优化实验区结论，供追溯"某个数字是怎么来的"、"哪些质量问题人工确认过"。其中的中间统计数字多数已被后续版本取代，引用前务必核对日期。

## 1. C 检测数据清理、去重与重新划分（2026-07-18 较早时点）

> **本节数字已被取代。** 用户随后再次整理了 `data/C/`，当前实际为 train 1,214 / val 152 / test 152 张、4,665 + 583 + 583 个框。本节记录的 1,110/229/227 张与 6,051 个框属于中间版本。
> **本节的 class 0/1 指代与现行定义相反。** 当时的类别顺序沿用旧的 `0=correcta`、`1=dorsalis`；现行统一定义为 `0=dorsalis`、`1=correcta`。本节所有分类别计数按当时顺序阅读。

C 数据集已完成标签冲突处理、无配对样本清理、完全重复去除和重新划分：

- 用户人工确认 99 组冲突标注采用 test 侧标注；已覆盖 98 个 train/val 标签并补建 1 个 val 标签。418 个完全重复组的标签分类由"0 组完全一致、319 组语义近似、99 组实质冲突"变为"99 组完全一致、319 组语义近似、0 组实质冲突"；
- 用户决定不补标剩余 4 张缺标签图片；已永久删除这 4 张图片和 1 个孤立空标签。清理后图片缺标签、标签缺图片、空标签和标签格式异常均为 0；
- 对清理后的 1,985 张图片按 SHA-256 去重，保留 1,566 张唯一图片，去除 418 个重复组中的 419 个冗余副本；去重前曾保留服务器回滚目录，用户确认本地已有上传原版后已删除该备份；
- 以文件名推断的时间/采集段作为硬分组，按约 70%/15%/15% 重划为 train 1,110 张、val 229 张、test 227 张；dHash 近似关系只作为软约束和人工复核候选，不作为自动删除依据；
- 该中间版本共 6,051 个框：train 4,190（当时 class 0 为 3,024、class 1 为 1,166），val 962（690/272），test 899（631/268）；
- 独立全量复核确认：1,566 张图片全部完整解码，1,566 个标签全部一一配对，标签格式异常、空标签、重复标注行、SHA-256 完全重复和跨划分完全重复均为 0；
- 仍有 146 对跨划分 dHash 汉明距离不大于 2 的近似候选（train-val 73、test-train 67、test-val 6）。这些只是人工复核候选，不能直接视为重复或错误；
- 服务器端最终清单目录 `C_resplit_records_final_20260718_124548/` 已在用户确认本地有备份后删除；如需逐图去重映射或 146 对候选明细，应从用户本地备份恢复或重新计算。第一次试划分副本、旧版中间记录和一次性脚本也已清理。

该划分解决了原有的完全重复泄漏和结构问题，但源端没有提供真实视频/采集批次的逐图来源清单。"时间/采集段"是根据文件名构造的可复现代理分组，不能写成已证明所有同源连续样本都被严格隔离。在作为论文正式冻结划分前，应人工复核 146 对 dHash 候选，并确认文件名分组与真实采集批次的对应关系。

## 2. R1 真实场景检测数据审计、标签替换与分组重划（2026-07-17 至 2026-07-18）

> R1 的当前 train/val/test 张数与框数（4,458/798/960 张，4,253/723/723 框）与本节末次记录一致，可直接对照；本节前半部分的旧标签统计已被取代。

上传完整性和数据质量审计，以及用户人工补充漏标的合并：

- 上传完整性通过：6,216 张 JPEG 与 6,216 个标签数量、原始字节基线、配对和全量解码均通过；
- 原审计初步目视清单包含 9 张疑似漏标图片。用户逐图人工核查后，确认其中 8 张共漏标 13 个实蝇目标，对应 8 个非空标签已合并回 `data/R1/`；
- 用户确认 `images/test/006982.jpg` 中的对象为蚂蚁而非实蝇，因此 `labels/test/006982.txt` 保持空标签是正确的，不再列为漏标；
- 合并后 R1 仍为 6,216 张图片和 6,216 个标签，共 1,330 张正样本、4,886 个空标签和 2,132 个标注框；配对、类别、格式和坐标复核均通过；
- 合并和复核完成后，用户已授权删除人工核查目录和合并前的临时空标签回滚备份；
- **上述 1,330 个正样本、4,886 个空标签和 2,132 个框是旧标签版本的历史统计**，已被用户随后上传的新标注版本取代。

2026-07-18，用户以重新修改后的单类标签替换 R1，并授权在检查通过后合理重划。该版本已完成全量审计和序列组级重划：

- 6,216 张图片和 6,216 个标签，共 3,160 张正样本、3,056 个空标签和 5,699 个 `Bactrocera dorsalis` 框；全部图片均为可完整解码的 2,304×1,296 RGB JPEG；配对、类别、格式、数值、重复标注和 SHA-256 完全重复检查均通过；
- 21 行贴图像上边缘的框因六位小数舍入产生约 0.000648 像素负越界，已在保留全部原始标签压缩备份后精确夹紧到边界；图片内容未修改；
- 没有逐帧随机拆分。编号小于 `002989` 的数据按编号间隔不超过 10 的连续段构造保守代理组；后续数据结合源图精确哈希命中、连续块和原视频画面指纹推断来源组；
- `08-36-55` 与 `09-36-56` 两段空场景高度相似，已作为必须同集合组统一放入 train。最终 train/val/test 为 4,458/798/960 张，正样本为 2,228/492/440，空标签为 2,230/306/520，框数为 4,253/723/723；
- 全数据仍有 5,900 对相邻编号图片，最终仅 6 对跨集合，且均位于推断的来源组边界。跨集合 32×18 缩略图 PSNR≥40 dB 只剩 1 对；其全分辨率 RGB PSNR 为 22.77 dB、目标位置不同，不是重复帧；
- `data/R1/` 曾被用户误删，随后已从完整源副本 `/home/admin1/桌面/flydataset/R1/` 按最终序列分组映射恢复；6,216 张图片逐图 SHA-256 与源副本一致并全部完整解码，划分与标签统计不变；
- 原重划记录目录曾随数据被用户误删；重新生成的恢复记录目录随后也已按用户要求删除。当前正式数据和桌面源副本仍保留完整。

该划分可用于工程训练和对比实验，但还不能写成已由真实逐图来源清单证明的论文正式冻结划分：源端仍缺少"图片—原视频—原始帧号"的完整映射，现有来源组属于可复现代理；自动扩框后的 5,699 个框和 3,056 个空标签也尚未逐图人工复核。

## 3. `dataset_det_2` 实蝇框扩展重建（2026-07-18）

用户明确 `/home/admin1/桌面/flydataset/dataset_det/` 是标注基线，并要求用包含身体、翅膀和足的外接矩形重建 `/home/admin1/桌面/flydataset/dataset_det_2/` 的 JSON：

- 两个目录的 6,216 张 JPEG 已逐张 SHA-256 核对，图片完全一致；
- 基线目录共有 3,176 个 JSON、5,699 个框，其中 16 个 JSON 为空，另有 3,040 张图片没有源 JSON；
- 目标目录原有 6,216 个自动预测 JSON、11,605 个框，含大面积诱捕器误框、错误类别和低置信度预测，不能作为正式真值；
- 已以 `dataset_det` 为唯一标签基线重建目标目录全部 6,216 个 JSON。3,838 个框采用与旧预测严格匹配后的完整虫体候选边界，1,861 个不可靠匹配按原紧框方向扩展；无源标注的 3,040 张图片和 16 个源空 JSON 均输出为空标注，旧预测不再残留；
- 重建后共有 5,699 个框、3,160 个非空 JSON 和 3,056 个空 JSON；全部 JSON 可解析，全部新框包含原框、面积均已扩大，坐标均在 2,304×1,296 图像范围内；
- 旧目标 JSON 已备份至 `/home/admin1/桌面/flydataset/dataset_det_2_json_before_expansion_20260718_154604.tar.gz`。

本次扩框属于自动候选辅助的批量重建，已完成结构校验和抽样目视检查，但**不能写成 5,699 个框均经人工逐框确认**。用于正式训练或论文实验前，仍应对扩框结果做完整人工复核，重点检查重叠实蝇、诱捕器中心附近、图像边缘和运动模糊样本。

## 4. 边缘优化准备（实验区已于 2026-07-25 删除）

曾在 `raspberry_pi_optimization_experiments/` 建立隔离实验区，完成以下准备性工作：

- PyTorch 不同输入尺寸精度扫描；
- ONNX 和 NCNN 导出与服务器环境验证；
- 静态/动态 ONNX 与 NCNN 的服务器 CPU 基准；
- 树莓派候选方案和蒸馏计划；
- TFLite/INT8 TFLite 导出当时受 TensorFlow 依赖解析阻塞，未完成。

上述结果全部基于当时未冻结的临时权重和服务器 CPU/GPU，只能用于功能验证与候选筛选，从来不是树莓派结果。**该目录已由用户删除，相关脚本和数据不再存在于本仓库。** 树莓派实机验证仍是学位论文链路的一部分（见主文档阶段 E），但届时应在冻结检测器之后重新建立实验区，不复用上述已删除的准备产物。
