# 变更日志

> 文档性质：只追加的历史记录。新会话**不需要**通读本文档。
> 当前状态、待决策项和下一步见 `当前进展与下一步.md`；只有需要追溯"某个决定是什么时候、依据什么做出的"时才查本文档。
> 记录规则：每条注明日期、修改内容和依据；不把未证实的设想写成已完成事实。已失效的历史记录不删除，加失效标注。

## 2026-07-30

### BoT-SORT/LV006 正式缓存诊断视频

- 新增独立下游脚本 `scripts/render_track_cache_diagnostics.py`，按 `video_data/manifests/segments.csv` 的 LV006 权威顺序读取 7 个 BoT-SORT 正式轨迹缓存，并通过 `benchmark_common.numeric_cache` 完整校验 header、字段、cache ID、逐帧连续性和 footer；脚本未导入或运行检测器、tracker 或正式计数模块，画面计数和事件只重放原 `count_events.csv`；
- 在 tmux 会话 `botsort_lv006_render` 先完成 3,600 帧 pilot，抽取第 100、3,061、3,501 帧截图并人工确认框、圆、坐标缩放、文字、尾迹及正式 enter/exit 高亮正确；正式任务随后串行生成 7 段 `1152×648`、H.264、yuv420p 诊断视频，没有跳帧或并行渲染；
- 7 段源视频、轨迹缓存和输出帧数逐段一致，总计均为 1,785,593 帧；输出平均 FPS 与各源平均 FPS 最大绝对差 `2.66e-7`，2,944 条正式事件均映射到合法缓存帧并只应用一次，正式累计终值严格复现 dorsalis `27`、correcta `21`。每段转正前均经 ffprobe 逐帧计数和 ffmpeg 全量解码，7 行 `render_manifest.csv` 全部为 `PASS`，总输出 1,614,877,622 字节，目录无 `.partial.mp4`；
- 对 7 个源视频、7 个轨迹缓存和 4 个正式计数 CSV 建立前后快照，18/18 项大小与修改时间不变，4 个 CSV 的 SHA-256 也不变；事件索引、轨迹摘要、输入完整性表、日志和说明位于 `video_test/botsort_lv006/`；
- 只读摘要观察到 33,453 个 raw track，其中 28,065 个观测不超过 3 帧、3,987 个首次框中心在圈内、3,926 个缓存类别发生变化；46 个 raw track 的正式事件出现多个类别 ID，232 个事件轨迹至少触发 4 次正式跨界，单轨最多 66 次。这些数值只作为短轨迹、first-seen-inside、类别变化和边界反复穿越的人工定位线索；没有逐帧 ID 真值，不能据此确定 ID switch、轨迹断裂或 LV006 少计的因果归属。

## 2026-07-29

### RT-DETR-R18 + 8 tracker 正式分类计数 baseline

- 在 `bench_tracking` 环境完成正式运行前检查：counting 23 项、tracking 9 项测试通过；检测缓存仍为 25 个片段、10,007,059 帧，运行前后 `detection_cache_manifest.csv` 与 `verification_report.json` 的 SHA-256、大小和修改时间一致，未重跑、覆盖或修改检测缓存；
- 按固定 RT-DETR-R18 检测缓存、固定 `baseline_circle_v1.json`、每类初值 0 和各 tracker 正式默认配置，依次完成 SORT、ByteTrack、OC-SORT、SF-SORT、FastTracker、BoostTrack、HybridSORT、BoT-SORT；每个 tracker 覆盖 25 个片段和 10,007,059 帧，共完成 200 个片段轨迹结果、80,056,472 tracker-frame 处理量及 48 个逻辑视频—tracker 计数结果，输出位于 `project_results/tracking_counting_baseline_v1/`；
- 默认评价器在不使用 `--allow-partial-trackers` 或其他放宽开关的情况下通过；BoT-SORT 获得最低 `Class-MAE=17.166667`，其 `dorsalis MAE=25.5`、`correcta MAE=8.833333`、`Total-MAE=34.333333`、完全正确视频比例 `0.166667`、负计数比例 `0`；完整排名、逐视频误差和加权速度见 `project_results/tracking_counting_baseline_v1/evaluation/counting_metrics.csv`、`project_results/tracking_counting_baseline_v1/evaluation/per_video_species_errors.csv` 与 `project_results/tracking_counting_baseline_v1/tracking_speed_summary.csv`；
- 对 8×25 轨迹缓存调用严格只读复用校验并完整遍历检测/轨迹 JSONL：200/200 缓存、80,056,472 tracker-frame 的逐帧连续性、唯一 footer、上下游帧数、头信息、cache ID 和解析配置均通过；48/48 个 `final_counts.csv` 均恰好包含唯一的 class 0/1 两行，所有配置快照齐全且无临时/半成品文件；
- 8 个 tracker 均未崩溃；正式输出仅观察到 SF-SORT/LV006 的两个负计数（dorsalis `-16`、correcta `-33`），SF-SORT/LV005 也严重少计（`8/14` 对真值 `61/118`），其余算法的 LV006 dorsalis 预测为 `18--28`、均明显低于真值 79；有符号计数按协议未截断，这些结果作为 born-inside、轨迹碎片、边界徘徊或类别抖动的待诊断信号，不写成已确定因果；
- 本轮 6 个逻辑视频全部用于工程选型且 correcta 阳性仅覆盖 2 个视频，无独立测试集和野外逐帧 MOT ID 真值，因此结果只支持固定感知与计数条件下的下游计数 baseline 排名，不支持野外 HOTA/MOTA/IDF1 或独立数据泛化结论；协议 v2.1 仍待导师审查。

## 2026-07-28

### 跟踪计数 baseline 静默错误修复

- 将 SF-SORT 默认配置同步到 `MOT算法计数性能对比实验协议.md` v2.1：启用动态调参、补齐 `cth/high_th_m/match_th_first_m/new_track_th_m`，第二阶段阈值改为 0.20，timeout 和 margin 按每片段 FPS/画面尺寸向下取整；已解析整数 timeout override 不会再次乘 FPS。修改前两个适配入口均复现 `ValueError: sfsort 配置包含未知字段: ['cth', 'high_th_m', 'match_th_first_m', 'new_track_th_m']`，修改后完整协议 override、原生构造参数、4 帧 `cache_version=2` 端到端缓存和 `iter_cache_frames` 完整性校验均通过；
  - **本条已部分失效（2026-07-28）**：后续按 baseline 防误用要求删除固定整数 timeout 入口，正式配置只接受 `{"fps_ratio": ...}`，固定帧数不再作为合法 override。
- 删除计数重放和批处理的 `--initial-count` 参数、解析及透传，正式计数固定从每类 0 开始；实跑生成的 `replay_config.csv` 明确记录 `initial_count_policy=fixed_zero_per_class` 和 `initial_counts=0=0|1=0`，原 `replay_cache(..., region_counts, ...)` 跨片段累计接口保持不变；
- 计数重放的四个 CSV 和批处理 tracker 子目录/manifest 默认拒绝覆盖，只有显式 `--overwrite` 才允许重建；评价改为只读取 `benchmark_run_manifest.csv` 列出的 tracker/count_root，并对缺 manifest、无法唯一归属和同一逻辑长视频多个 `final_counts.csv` 全部报错并列出冲突路径。默认拒绝与显式覆盖的 CLI 路径均已实跑；
  - **本条已部分失效（2026-07-28）**：批处理 `--overwrite` 后续收窄为严格校验并复用轨迹、只重写计数 CSV；全删 tracker 目录改由名字明确且互斥的 `--purge` 执行。
- 最后一次适配器改动后，tracking 9 项、counting 19 项单元测试和 8/8 独立进程接口冒烟全部通过；未读取、重跑、覆盖或清理 `project_results/detection_cache/`，未运行正式真实长视频 baseline。

### 增量名单、安全重跑与 SF-SORT 公式约束

- `run_counting_benchmark.py` 的 `benchmark_run_manifest.csv` 改为加锁后原子读改写：不同调用完成的 tracker 行会累积保留，同名 tracker 默认报错；每次调用生成含微秒时间与随机后缀的唯一 `run_id`，同一调用内各 tracker 共用该批次 ID。现有名单与本次检测缓存完整路径或公开计数条件不一致时拒绝混写；分两次实跑 SORT、ByteTrack 后名单保留两行且批次 ID 不同；
- 批处理新增互斥的 `--overwrite/--purge`：前者先核对轨迹文件集合、完整 footer/逐帧连续性、检测 cache ID、帧数、类别、画面、FPS、跟踪器 commit 和解析配置，再仅删除四个计数 CSV 并复用轨迹；后者才删除所选 tracker 目录并从跟踪阶段重跑。实跑中 `--overwrite` 前后轨迹与 `tracking_run.csv` 哈希相同，`--purge` 产生新跟踪批次；人为增加额外轨迹缓存时复用预检报错，manifest 和已有计数文件哈希均未变化；
- SF-SORT 的 `marginal_timeout/central_timeout` 只接受 `{"fps_ratio": ...}`，固定整数 `38` 实跑报错，协议公式配置完成 4 帧 `cache_version=2` 端到端跟踪并通过 `iter_cache_frames`；最后一次改动后 tracking 9 项、counting 21 项单元测试与 8/8 独立进程 smoke 全通过。本轮未读取、重跑、覆盖或清理 `project_results/detection_cache/`，未运行正式真实长视频 baseline。
- 评价端新增协议 tracker 集合校验：默认要求 SORT、ByteTrack、OC-SORT、SF-SORT、FastTracker、BoostTrack、HybridSORT、BoT-SORT 8 个算法齐全，缺少算法时报错并列出 `missing`；`--allow-partial-trackers` 只用于分批诊断且会打印醒目警告，未知 tracker 即使开诊断模式仍拒绝。两算法临时 manifest 实跑确认默认退出码 1、诊断开关生成两行指标；最后一次改动后 counting 23 项、tracking 9 项测试通过。

## 2026-07-27

### RT-DETR-R18 统一长视频检测缓存

- 核验冻结 checkpoint 顶层同时含 `model` 与 `ema`，其中 `ema.module` 与模型同为 533 个 tensor、EMA updates=50,900；视频后端据此强制加载 EMA，不作静默回退。官方数据集源码明确 `orig_size=[w,h]`，后处理据此将 xyxy 乘以 `[w,h,w,h]`；源码 commit、权重 SHA-256、类别顺序和三步 val 预处理均在运行时校验；
- 新增独立 RT-DETR-R18 后端、manifest 驱动的长视频缓存入口、冻结 val 一致性入口和缓存集合严格校验入口。推理固定 FP32、batch 16、直接拉伸 640×640、原生 NMS-free top-300、两个类别均保留至 `confidence ≥ 0.10`；`--iou` 会被参数解析拒绝，缓存元数据记为 `not_applicable`；
- 完整重跑 1,530 张冻结 val 图片，与 `val_mixed.json` 的 430,357 行预测逐框比对，坐标、置信度、类别和输出顺序全部一致，最大绝对误差为 0；
- 按 manifest 的 `video_id + segment_index` 显式生成并校验 25/25 个检测缓存：实际解码帧总数 10,007,059，检测 8,817,155 个，空检测帧 6,269,427，25 个 footer 均完整且帧号连续；checkpoint SHA-256 为 `558b7fd2c7b6488649d64cada5d6c860841301d00b2afec7907f29eeef44cc6f`，集合 numeric payload SHA-256 为 `83d60d1e0ce66d77f948c4b3072426bba66faccb55fe224ec87ef68a8b64bd18`；
- 正式缓存共 550,443,689 字节（55.01 字节/帧，约 526 MiB）；先导片段 LV005:6 为 99.40 字节/帧，因此直接外推的约 0.995 GB 是保守高估。当前 counting 12 项测试全部通过；本次没有运行任何 tracker 或生成计数结果；
- 新增 `detection_model_benchmark/DETECTION_CACHE_HANDOFF.md`，将本轮已验证的执行命令、数值证据、通用后端契约、换检测器验收表、分片恢复方法和静默错误清单整理为后续检测前端替换的统一交接入口；`detection_model_benchmark/README.md` 与状态文档已建立索引。

## 2026-07-25

### 旧配置与临时权重归档（提交 `299cf05`）

- 根目录 `data.yaml`、`dataset_two_class.yaml`、`split_data.py`、`train.py` 及 `yolo8n_two-class.pt`、`yolo26n.pt` 移入 `legacy/`，按 `configs/`、`weights/`、`scripts/` 分类；`legacy/README.md` 记录归档原因、被取代关系和类别顺序风险。权重仍受 `*.pt` 规则忽略，不入版本控制；
- 登记此前从未记录的类别顺序冲突：`legacy/configs/dataset_two_class.yaml` 与 `legacy/weights/yolo8n_two-class.pt` 为 `0=correcta`、`1=dorsalis`，与统一定义 `0=dorsalis`、`1=correcta` **完全相反**。下游按物种名匹配真值时对调不报错，只会得到两物种数值互换的结果。历史 BoxMOT 输出给出 correcta=93 / dorsalis=45，而对应真值为 dorsalis=61 / correcta=118，方向恰好相反，与此一致；
- `detection_model_benchmark/cache_detections.py` 三处修正：`DEFAULT_NAMES` 改为与 `classes.yaml` 一致；移除缺少 `best.pt` 时会静默回落到历史权重的兜底链，改为要求显式 `--weights`；新增 `check_class_order`，权重 `names` 与 canonical 定义冲突时直接失败。此前 `best.pt` 确实不存在，任何不带 `--weights` 的调用都会选中反序权重；
- counting 10 项与 tracking 6 项单元测试通过。

### 研究文档事实订正与结构拆分

- 拆分为三份：`当前进展与下一步.md`（当前状态，新会话必读，正文限 120 行）、本变更日志（只追加）、`数据集沿革归档.md`（C/R1/dataset_det_2 清理过程与边缘优化准备）。`AGENTS.md` 同步登记三份文档的分工与状态文档的行数/写法约束，防止状态文档再次只增不减；
- 状态文档同步登记 `MOT算法计数性能对比实验协议.md` v2.0（导师审阅版）的口径：本轮跟踪器选型固定 RT-DETR-R18 为检测前端、6 个逻辑长视频全部用于工程选型且暂不划独立测试集、主指标 `Class-MAE`，状态为待审查未冻结未运行；同时登记该协议列出的工程缺口（检测缓存入口无法加载 RT-DETR 原始 `best.pth`、`cache_id` 未覆盖数字 payload、SF-SORT 未启用官方在线轨迹找回参数、片段覆盖校验与 FPS 汇总待补）；
- 订正下列不实或矛盾记录：历史结果目录"保留为诊断证据"与"已清理"的矛盾；"真实前 6 帧全链路"降级为待验证（三层链路至今未产出任何 `.detections.jsonl`/`.tracks.jsonl`）；"8/8 接口冒烟"标注需重跑（冒烟日志 20:18:41 早于 `tracker_adapters.py` 最后改动 20:50:24）；`ablations/` 从"当前关键代码"移出（仅有 README 占位）；失效路径 `dataset/`、`data/C/C.yaml`、`data/R1/R1.yaml` 更正为实际配置位置；缓存协议"两层"更正为三层。

### 检测重跑与计数 baseline 决策

- 使用 R2 类别修正后的当前 test 数据视图，对 11 个冻结 best checkpoint 独立重跑 FP32 推理；11/11 返回 0，11 份新旧预测逐字节一致，mixed/C/R1/R2 中央指标最大绝对差为 0。新日志包含逐模型命令、GPU、checkpoint 哈希、batch 进度、预测哈希和返回码；原结果未覆盖；
- 用户最终决定先以各算法官方默认参数寻找计数 baseline，再在验证集微调。已撤回短暂采用的统一 `0.25` 高分阈值和 SORT 人工分数过滤：SORT 使用构造器默认 `max_age=1/min_hits=3/iou=0.3`；ByteTrack 使用主评测默认 `0.6/30/0.9`；OC-SORT 使用主入口与构造器默认且 `use_byte=false`；SF-SORT 使用构造器默认；FastTracker 使用注册 YAML；BoostTrack 使用默认 MOT17 分数参数；HybridSORT 使用官方 MOT17 no-ReID 实验参数；BoT-SORT 使用主评测默认。共享缓存下限仍为 `0.10`，只负责不提前丢信息。ReID、外观嵌入、GMC/ECC 等纯数字缓存无法提供的功能继续关闭；上述 baseline 尚未产生正式计数结论。

### 计数链路重构

- 用户撤回旧 B0/E1/E2/E3、容差/时序确认、类别投票及与这些策略绑定的旧轨迹缓存重放方案；相关代码、脚本、测试及两份旧协议文档已删除。**历史 BoxMOT/B0 计数输出与旧分片诊断输出同时从 `project_results/` 清理**（约 296.6 MB），不再作为当前诊断输入；
- 用户明确后续使用 `tracking_model_benchmark/` 中 8 个候选算法分别开展计数实验，根目录不再把 ByteTrack 作为默认跟踪器。旧 `main.py` 仅是 ByteTrack 计数可行性验证入口，现已删除；检测缓存入口迁入 `detection_model_benchmark/cache_detections.py` 并改为全帧检测。计数核心、重放、批处理、评价、版本化配置、消融目录和计数测试集中到 `counting_model_benchmark/`，三层共用数值缓存协议迁入 `benchmark_common/`。公共层已实现固定 `Nx6` 检测输入、`Nx8` 轨迹输出、8 个官方适配器和同一计数器；ROI 暂不纳入首轮 baseline；
- 已通过：16 项单元测试（counting 10 + tracking 6）。**待验证**：8 个官方算法的独立进程 `Nx6→Nx8` 接口冒烟需在适配器最后改动后重跑；真实前 6 帧全帧检测到 8 算法计数的全链路尚未执行，至今没有任何检测或轨迹缓存产物；
- 为第一篇论文/旧实验复现保留根目录 `boxmot/`；候选参数、检测器、数据划分、`N_start` 和正式 baseline 结果仍未冻结；
- 早前根目录工程入口曾采用全画面 YOLO + BoxMOT ByteTrack + 统一圆计数，并通过 10 项测试与真实 6 帧全画面 CPU 冒烟验证；该 ByteTrack 直连结构已被本日后续决定替代，不再是当前入口；
- 早前曾为通用缓存协议配置根目录 ByteTrack 专用跟踪重放；缓存格式、完整性校验和统一计数重放继续有效，但 `replay_tracking.py` 与 `tracker_runtime.py` 已被本日后续决定删除。

### 计数真值语义与视频规范化

- 用户确认 6 个逻辑长视频的目录级逐物种真值是全部片段拍摄结束后瓶内的最终数量；真值清单更新为 v1.1。计数代码统一改为有符号增量累计，退出事件允许使预测为负，不再用 `max(0, ...)` 截断；片段边界仍重置 tracker，同一逻辑长视频内计数状态连续；
- 将真实计数数据统一整理为 6 个逻辑长视频、25 个物理片段；目录名显式记录 dorsalis/correcta 目录级累计人工总数，文件名统一增加片段编号并消除 `output.mp4`、`two-class_4_3.mp4` 等歧义命名；
- 建立逻辑视频、片段、逐物种真值和未冻结划分清单。25/25 视频全量媒体探测、清单交叉校验以及重命名前后文件身份核验均通过；本次没有修改视频字节；
- 明确正式评价单位为 6 个逻辑长视频，不能把 25 个片段当成独立评价样本；目录级真值已冻结为最终瓶内占用数，development/validation/test 划分仍未冻结。

## 2026-07-22

- 明确第二篇论文只摘取"真实诱捕长视频分类计数"这一条证据线，负责回答"最终能不能数准"；跨场景检测器作为冻结前端条件，树莓派部署保留为学位论文完整链路和系统验证内容；
- 11/11 个模型均已完成统一 100 epoch 训练；每个模型的 100 行 val/optimizer 历史、50,900 次 optimizer step、12 个规范检查点和运行时配置审计全部通过；
- 在固化 best 检查点、收敛状态、空的 e160 扩展候选和 97 项配置清单后建立 `test_access_log.json`，随后完成 11 个模型的独立 test 推理；test 未用于阈值选择或模型调参；
- 首次 test 汇总的异常低分被追溯到 `R2/test` 的 0/1 类别真值颠倒。用户人工确认后，仅交换 405 个源/统一标签的类别首列并同步 mixed/R2 COCO 真值；原标签和首次错误表已留档。11,190 张数据全量验证、9,673 个 train/val 标签、16 个 train/val COCO 文件和 97 项冻结配置的复核均通过；新数据指纹为 `f84b7383...cb47`；
- 未重训、未重新推理，复用并逐文件哈希验证 11 份既有 test 预测完成中央重算。修正后的 mixed test `mAP50-95` 前四为 RT-DETR-R18 67.55%、YOLO12n 67.37%、YOLOv8n 66.81%、YOLOv10n 66.78%；旧的 38.57% 最高分和"R2 严重崩塌"结论作废；
- 后续依赖审计发现并修复 6 个活动原生 test 标签缓存的陈旧类别。原缓存已完整备份，修复后每个缓存均与当前 1,517 张 test 标签的类别和框坐标一致；未重训、未重新推理或重算指标；
- 本轮仍只是 seed 42、统一 100 epoch 的首轮工程筛选，尚未冻结第二篇论文最终检测器，下一步转为前列候选多 seed 与树莓派实测。

## 2026-07-21

- 除 YOLOv10n 外的 10 个模型已全部完成 100 epoch；各自的 100 行 val/optimizer 历史、12 个规范检查点和运行时配置审计均通过；
- YOLOv10n 的阻塞原因确认为 NumPy 2 标签缓存与冻结 NumPy 1.x 环境的 pickle 模块路径不兼容；通过薄适配层把其缓存读写重定向到模型本地目录解决，未修改公共数据缓存、Conda/Python 包、权重或冻结训练配置；
- 已用独立、启用的 user systemd 服务在物理 GPU1 单独启动 YOLOv10n。首个 epoch 已完成 8,143 张训练图、509 个 batch/optimizer step、逐 epoch val 和 `last/best` 保存，并进入第 2 个 epoch；独立 test 仍未执行。

## 2026-07-20

- RT-DETR R18 与 Faster R-CNN R50-FPN 已各完成 100 epoch；两者均有完整 100 行 val/optimizer 历史、50,900 次 optimizer step 和 12 个规范检查点，恢复审计确认已有文件未被重跑或覆盖；
- 已将会话依赖的旧调度器迁移为启用 linger 的 user systemd 服务 `fruitfly-benchmark-e100-s42.service`，并增加磁盘状态对账、完成态证据校验、队列重复领取防护和持续心跳/退出/异常记录；
- 恢复时修复了旧 YOLO 将逻辑 `cuda:0` 错映射到物理 GPU0、可选 Albumentations 引入协议外增强，以及 DEIM val DataLoader 的 `ancdata` 错误；修复均位于薄适配/调度层，未修改数据、权重、冻结训练配置或 Conda/Python 包；
- 09:39 的受控 systemd 重启确认 DEIM 与 YOLOv9 均从各自 `last` 正确续训；当前 GPU0 的 DEIM-D-FINE-N 与 GPU1 的 YOLOv9-T 已完成至少 2/8 个完整 train/val epoch 并继续运行，另外 7 个模型仍按冻结 FIFO 等待；test 未访问，全部实验尚未完成。

## 2026-07-19

- 已冻结 11 模型统一首轮筛选协议：100 epoch、seed 42、640 输入、有效 batch 16、弱增强、逐 epoch val、按纯 val `mAP50-95` 选 best，并在全部训练终态前隔离 test；
- 11/11 个模型的单批训练反传、优化器更新和单批验证预检全部通过；配置清单、静态校验和模型启动时运行时审计通过，过程未改变任何 Conda/Python 包；
- 正式调度已于 16:22 启动，GPU0 运行 Faster R-CNN R50-FPN，GPU1 运行 RT-DETR R18，其余 9 个模型进入冻结队列；当前实验仍在进行，尚不能形成性能排名、test 结论或最终模型选择。

## 2026-07-18

- 已从用户当前整理完成的 C/R1/R2 构建 `datasets/fruitfly_detection_v1/`：11,190 张图、19,540 个框、3,060 张空背景图，组合 train/val/test 为 8,143/1,530/1,517；类别统一为 `0=dorsalis`、`1=correcta`；
- 已生成 YOLO、DEIM/RT-DETR 0-based COCO 和 Faster R-CNN 1-based COCO 三套一致视图，并完成全量解码、哈希、标签、重复、逐框转换、分域列表和官方加载器验证；数据集指纹为 `a56fa4a6e10370e963d34e983711c3ed69b5b2f064a0d7771d976d6f1c57b5da`（后经 R2-test 类别修正，当前指纹为 `f84b7383...cb47`）；
- 已为 11 个模型写入实际 `dataset_config.yaml` 和公共数据路径；Faster R-CNN 薄适配器默认保留空背景训练图，避免官方参考入口自动删除 2,234 张空训练图造成样本不一致；本轮未启动任何模型训练；
- C/R1/dataset_det_2 的清理、审计与重划细节见 `数据集沿革归档.md`。

## 2026-07-16

- 新建 `project_results/` 作为本项目计数与跟踪结果的统一存放目录，并迁入当时的 BoxMOT 历史计数结果、轨迹缓存和 track 级诊断证据；仅调整存放路径，不改变实验状态、数据定义或结论边界。
- **本条已部分失效（2026-07-25）**：上述迁入的对象已全部从 `project_results/` 清理。其中 6 个 BoxMOT 汇总 CSV 仍可从提交 `f95ffa9` 取回；`result_counting_cache/` 与 `result_two_class_79_27_individual/` 从未纳入版本控制，已不可恢复。

## 2026-07-15

- 在大论文新主线下，第二篇论文检测模型尚未确定，现有 `yolo8n_two-class.pt` 仅作为工程临时权重（2026-07-25 已移入 `legacy/weights/`，且其类别顺序与统一定义相反）；
- 《计数消融实验协议》及 B0/E1/E2/E3 明确为暂定的工程验证与诊断方案，不作为已冻结论文实验（2026-07-25 已由用户撤回）；
- 建立本项目的两份长期背景/动态状态文档；
- 将学位论文主线收缩为跨场景检测、实验室标准 MOT、真实长视频分类计数三个研究目标；
- 确认实验室数据用于验证跟踪机制，真实数据用于验证下游分类计数价值；
- 确认领域差异融入统一检测研究，不单独作为必须研究章；
- 明确实验室运动只可表述为覆盖真实场景的代表性运动挑战，不可声称两者运动分布一模一样；
- 删除已被当前三目标方案替代的 `研究主线与两篇论文关系.md`，并将其中仍有效的 track 级计数诊断证据迁移至进展文档。
