# Configuration placeholder

`counting_candidate_v1.json` is the runnable pre-tuning BoostTrack candidate, not a validation-frozen configuration. Create a separately named formal config after validation tuning; cached detections, `per_class=false`, `use_embedding=false`, and `use_ecc=false` remain mandatory.
