# Configuration placeholder

`counting_candidate_v1.json` is the runnable pre-tuning FastTracker candidate copied from the registered runtime defaults. It is not validation-frozen. Create a separately named formal config after validation tuning; one class-agnostic tracker with no ReID, GMC/ECC/optical flow, future frames, or offline post-processing remains mandatory.
