# Configuration placeholder

`counting_candidate_v1.json` is the runnable pre-tuning BoT-SORT candidate, not a validation-frozen configuration. Create a separately named formal config after validation tuning; cached detections, `per_class=false`, `with_reid=false`, and `cmc_method=none` remain mandatory.
